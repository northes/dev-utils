import {useEffect, useMemo, useRef} from 'react'
import {useTranslation} from 'react-i18next'
import {ArrowsClockwise, Clock, Copy} from '@phosphor-icons/react'
import {Reveal, ToolHeader, undoRedoKey, useHistory, type PendingAction, type ToolId} from './shared'
import {toast} from './AppToast'

export default function TimeTool({active,record,pending,clearPending}:{active:boolean;record:(tool:ToolId,action:string,detail:string,input:string)=>void;pending:PendingAction|null;clearPending:()=>void}){
  const{t}=useTranslation();const{value:input,setValue:setInput,undo,redo}=useHistory('1786358400');const consumed=useRef<PendingAction|null>(null);const parsed=useMemo(()=>{const raw=input.trim();if(!raw)return null;const numeric=Number(raw);const date=/^\d{10,13}$/.test(raw)?new Date(raw.length===10?numeric*1000:numeric):new Date(raw);return Number.isNaN(date.getTime())?null:date},[input]);const values=parsed?[[t('timeTool.unixSeconds'),Math.floor(parsed.getTime()/1000).toString()],[t('timeTool.unixMilliseconds'),parsed.getTime().toString()],[t('timeTool.unixNanoseconds'),(BigInt(parsed.getTime())*1000000n).toString()],[t('timeTool.rfc3339'),parsed.toISOString()],[t('timeTool.localTime'),parsed.toLocaleString()]]:[]
  useEffect(()=>{if(!pending||pending.tool!=='time'||consumed.current===pending)return;consumed.current=pending;clearPending();setInput(pending.input,{isolate:true});if(pending.action!=='restore')record('time',t('timeTool.converted'),pending.input.trim().slice(0,40),pending.input)},[pending])
  return <Reveal index={0} active={active}><section className="tool-page"><ToolHeader title={t('timeTool.title')} desc={t('timeTool.subtitle')}/><label className="field"><span>{t('timeTool.input')}</span><div className="field-control"><Clock size={18} weight="duotone"/><input value={input} onKeyDown={e=>undoRedoKey(e,undo,redo)} onChange={e=>setInput(e.target.value)} placeholder={t('timeTool.placeholder')}/><button className="icon-button" aria-label={t('timeTool.refresh')} title={t('timeTool.refresh')} onClick={()=>{const now=Math.floor(Date.now()/1000).toString();setInput(now,{isolate:true});record('time',t('timeTool.refresh'),now,now)}}><ArrowsClockwise size={15} weight="duotone"/></button></div><small>{t('timeTool.hint')}</small></label><div className={`detected ${parsed?'':'invalid'}`}><span>{t('timeTool.detected')}</span><strong>{parsed?(/^\d{10}$/.test(input.trim())?t('timeTool.unixSec'):/^\d{13}$/.test(input.trim())?t('timeTool.unixMs'):t('timeTool.dateString')):t('timeTool.unrecognized')}</strong></div>{parsed?<div className="conversion-list">{values.map(([label,value])=><div key={label}><span>{label}</span><button aria-label={t('timeTool.copy',{label})} title={t('timeTool.copy',{label})} onClick={()=>{navigator.clipboard?.writeText(value);toast(t('toast.copied',{value}));record('time',t('timeTool.converted'),label,input)}}><code>{value}</code><Copy size={15} weight="duotone"/></button></div>)}</div>:<div className="empty-state compact"><Clock size={24} weight="duotone"/><strong>{t('timeTool.emptyTitle')}</strong><span>{t('timeTool.emptyHint')}</span></div>}</section></Reveal>
}
